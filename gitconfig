[alias]
	b = branch
	c = commit
	co = checkout
	d = diff
	ds = diff --staged
	l = log --oneline
	s = status --short --branch

[include]
	path = .gitconfig.local

[merge]
	conflictstyle = diff3
	tool = vimdiff

[push]
	default = simple
